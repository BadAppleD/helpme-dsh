# HelpMe DSH plugin

This plugin bundles a concise `SessionStart` routing hint and a self-describing local DSH MCP bridge. It does not install a Skill or Codex custom subagent. See the repository [README](../../README.md) for installation, configuration, and security guidance.
